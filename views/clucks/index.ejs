<%- include("../partials/header") %>
<section class="clucks-container">
    <%for (let cluck of clucks) {%>
        <div class="cluck-container">
            <div class="profile-img-container">
                <img src="/images/cluck-profile-image.jpeg" alt="no image yet" style="width:60px; height:60px">
                <p><%= cluck.username + ' • ' + cluck.created_at %></p>
            </div>
            <div class="body-container">
                
                <p><%= cluck.content %></p>
                <img src= <%= cluck.image_url %> alt="" class="cluck-image">
            </div>
        </div>
    <%}%>
</section>
<%- include("../partials/footer") %>